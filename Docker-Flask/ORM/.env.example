# Copia este archivo como ".env" (cp .env.example .env) y cambia el valor
# por una llave secreta aleatoria antes de levantar el servicio.
# NUNCA subas tu archivo .env real al repositorio.
JWT_SECRET_KEY=cambia-esto-por-una-llave-larga-y-aleatoria
JWT_EXPIRES_HOURS=2
